# Base URL of the LastHeard API (Cloudflare Worker).
# In dev this defaults to http://localhost:8787 (wrangler dev).
# For production builds, set this to your deployed Worker URL, e.g.:
#   VITE_API_BASE=https://lastheard-api.<your-subdomain>.workers.dev
VITE_API_BASE=http://localhost:8787